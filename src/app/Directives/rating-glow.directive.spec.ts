import { RatingGlowDirective } from './rating-glow.directive';

describe('RatingGlowDirective', () => {
  it('should create an instance', () => {
    const directive = new RatingGlowDirective();
    expect(directive).toBeTruthy();
  });
});
